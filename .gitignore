# python
backend/.venv/
__pycache__/
*.pyc
.pytest_cache/
# captures, failed frames and grower records — never commit fruit photos or
# farmer names. A trailing comment would become part of the pattern, so these
# stay on their own lines.
backend/data/
data-backup/
backend/synthetic.jpg

# node
node_modules/
frontend/dist/
frontend/dev-dist/
.vite/

# env — the tunnel URL is not a secret, but keep local overrides out
.env
.env.local
!frontend/.env.example

# os / editor
.DS_Store
*.swp
.idea/
.vscode/
frontend/.git.bak-scaffold/
