# รัน PoC บน MacBook

โหมดนับด้วยมือ สร้างเสร็จแล้ว เอกสารนี้คือขั้นตอนเปิดใช้

---

## 1. เปิดเซิร์ฟเวอร์

```bash
cd "AI Smart Fruit Grading/backend"
PORT=8010 ./run-local.sh
```

> พอร์ต 8000 มีเซิร์ฟเวอร์ Python ตัวอื่นของคุณใช้อยู่ จึงใช้ 8010 แทน

พิมพ์ token ออกมาบนจอ — **จดไว้ ต้องใช้ในขั้นที่ 3**
(เก็บถาวรใน `data/.api-token` ไม่เปลี่ยนทุกครั้งที่รีสตาร์ต)

ตรวจว่าเก็บข้อมูลได้จริง:

```bash
curl -s localhost:8000/api/health | python3 -m json.tool | grep persisting
```

ต้องได้ `"persisting": true` — ถ้าเป็น `false` ข้อมูลกำลังหายอยู่

> `caffeinate` อยู่ในสคริปต์แล้ว ปิดฝาแล้วเซิร์ฟเวอร์ไม่ตาย

---

## 2. เปิด tunnel

**ต้องใช้ HTTPS** หน้าเว็บอยู่บน Vercel ผ่าน TLS ยิงไปที่ `http://192.168.x.x`
เบราว์เซอร์บล็อกทิ้งเป็น mixed content — ต่อ LAN ตรง ๆ ไม่ได้ ไม่ว่าจะเร็วแค่ไหน

### ที่ใช้อยู่ — ngrok, URL คงที่ ไม่ต้องซื้อโดเมน

```bash
cd "AI Smart Fruit Grading/backend"
PORT=8010 ./tunnel.sh
```

ได้ `https://oilless-superurgently-mafalda.ngrok-free.dev` **ทุกครั้ง** ไม่เปลี่ยน
สคริปต์อ่านชื่อโดเมนจาก `backend/.env` ซึ่งไม่ได้อยู่ใน git — เครื่องใหม่ต้องใส่เอง:

```
NGROK_DOMAIN=<โดเมนที่จองไว้>
```

⚠️ **บัญชีฟรีให้ agent ทีละตัว** โดเมนนี้ใช้ร่วมกับ ai-agent-server ใครเปิดทีหลังได้ไป
ถ้าจะรันพร้อมกันต้องอัปเกรด หรือให้ตัวใดตัวหนึ่งไปใช้ Cloudflare แทน

### ทางเลือก — Cloudflare (ต้องมีโดเมนของตัวเอง)

ไม่จำกัดจำนวน agent เหมาะกว่าถ้าจะใช้ยาวทั้งฤดู ทำครั้งเดียว
**ต้องรันเองเพราะเปิดเบราว์เซอร์ให้ล็อกอิน**

```
! cloudflared tunnel login
! cloudflared tunnel create kat-pon-dee
! cloudflared tunnel route dns kat-pon-dee kpd.your-domain.com
```

จากนั้นทุกครั้ง:

```bash
PORT=8010 ./tunnel.sh kpd.your-domain.com
```

### ทางที่ควรเลี่ยง — quick tunnel

ถ้าไม่ตั้ง `NGROK_DOMAIN` และไม่ใส่ชื่อโดเมน สคริปต์จะถอยไปใช้ Cloudflare quick
tunnel ซึ่ง**เปลี่ยน URL ทุกครั้งที่รีสตาร์ต** ต้องไปกรอกใหม่ในมือถือทุกเครื่อง
และการรีสตาร์ตมักเกิดตอนยืนอยู่หน้าตะกร้าพอดี

---

## 3. ตั้งค่าในแอป

เปิด `https://kat-pon-dee.vercel.app` → **ตั้งค่า**

| ช่อง | ใส่อะไร |
|---|---|
| ที่อยู่เซิร์ฟเวอร์ | URL จาก tunnel |
| API token | token จากขั้นที่ 1 |

หรือใส่ใน Vercel เป็น `VITE_API_URL` และ `VITE_API_TOKEN` แล้ว redeploy
ตอนนี้ URL คงที่แล้วจึงใส่ใน Vercel ได้ ไม่ต้องกรอกทีละเครื่อง
**ยกเว้นถ้าถอยไปใช้ quick tunnel** — URL เปลี่ยนทุกวัน ใส่ในแอปสะดวกกว่า

---

## 4. สำรองข้อมูล

**อย่าข้าม** ชุดภาพลำไยจริงคือสิ่งเดียวที่ปลดล็อกโมเดลระยะ 3 และเก็บใหม่ไม่ได้
เมื่อหมดฤดู

```bash
./backup-data.sh /Volumes/Backup/kat-pon-dee
```

### อัตโนมัติทุกชั่วโมง — ⚠️ ต้องให้สิทธิ์เอง

โปรเจกต์อยู่ใน `~/Documents` ซึ่ง macOS ป้องกันไว้ **ทั้ง cron และ launchd
อ่านไม่ได้จนกว่าจะให้ Full Disk Access** ผมลองติดตั้งแล้วได้ exit 126 จึงถอดออก
เพราะตัวสำรองที่ล้มเหลวเงียบ ๆ ทุกชั่วโมงแย่กว่าไม่มีเลย

ไฟล์ agent เตรียมไว้แล้วที่ `~/Library/LaunchAgents/com.katpondee.backup.plist`

**วิธีเปิดใช้:**

1. System Settings → Privacy & Security → **Full Disk Access**
2. กด `+` แล้วเพิ่ม `/bin/bash`
3. โหลด agent:

```
! launchctl bootstrap gui/$UID ~/Library/LaunchAgents/com.katpondee.backup.plist
```

4. ตรวจว่าทำงานจริง — **อย่าเชื่อว่ามันรัน จนกว่าจะเห็นบรรทัดใหม่ในล็อก**

```bash
tail -f backend/data/backup.log
```

**ถ้าไม่อยากให้สิทธิ์** ก็รันมือทุกครั้งที่เก็บข้อมูลเสร็จ — ปลอดภัยกว่าถ้าจำได้:

```bash
./backup-data.sh "/Users/plug/Documents/Project/AI Smart Fruit Grading/data-backup"
```

---

## 5. ใช้งานโหมดนับ

หน้าแรก → **นับและคัดเกรด** (ขึ้นเฉพาะเมื่อเลือกแผ่น 500 มม.)

```
วางแผ่นบนโต๊ะ · มือถือบนขาตั้งเหนือแผ่น
        │
        ▼
กด "เริ่มรอบนับ"
        │
        ▼
┌──────────────────────────────────────┐
│ เทลำไย 15–20 ลูก ลงช่องซ้ายหรือช่องบน │
│ เกลี่ยด้วยนิ้วให้ไม่ติดกัน              │
│ ปล่อยมือ → วงสีขึ้นภายใน ~0.5 วิ       │
│ ปัดทั้งแถวไปทางขวา → ตัวนับเด้ง +N     │◄─┐
└──────────────────────┬───────────────┘  │
                       └──────────────────┘
        │
        ▼
กด "จบรอบนับ" → ยอดถูกบันทึกไว้แล้วตลอดทาง
```

ตระกร้า 500 ลูก ≈ 25–30 รอบ ≈ 3–5 นาที **ได้สำมะโนครบทุกลูก ไม่ใช่การสุ่ม**

---

## 6. ข้อควรรู้

| | |
|---|---|
| **ใช้ได้เฉพาะแผ่น 500 มม.** | A3/A4 พื้นที่ไม่พอ แอปซ่อนเมนูให้เอง |
| **นับตอนลูกหายไปจากพื้นที่วัด** | หยิบออกทางอื่นก็นับ — มีปุ่มถอนคืน |
| **ถ้าลูกกลับมาที่เดิม ระบบถอนคืนเอง** | กัน detector พลาดหนึ่งเฟรมแล้วนับซ้ำ |
| **ลูกติดกันไม่นับ** | แอปเตือน "ติดกัน N จุด" ต้องเกลี่ยก่อน |
| **ก้ำกึ่งไม่เดาเกรด** | ห่างเส้นแบ่งน้อยกว่า 0.3 มม. → แสดง `?` |
| **โหมดสดหยาบกว่าถ่ายนิ่ง** | ใช้ช่วยคัด ถ้าจะเป็นหลักฐานให้ถ่ายนิ่ง |

---

## 7. ⚠️ ยังไม่ได้พิสูจน์

**ยังไม่เคยวัดลำไยจริงสักลูก** ตัวเลขความแม่นทั้งหมดมาจากทรงกลมสังเคราะห์ที่เรนเดอร์
ด้วยแบบจำลองเรขาคณิตชุดเดียวกับที่ pipeline ใช้แก้ — พิสูจน์ว่าโค้ดสอดคล้องกับตัวเอง
ไม่ได้พิสูจน์ว่าตรงกับของจริง

ก่อนให้ชาวสวนใช้จริง ต้องทำ **ขั้น 6** ของ `docs/poc-plan.md`:

1. นับด้วยมือ 200 ลูก เทียบกับที่แอปนับ → นับขาด/เกินกี่ลูก
2. วัดด้วยเวอร์เนีย 50 ลูก เทียบกับที่แอปวัด → bias จริงเท่าไร

---

## 8. ย้ายไป DigitalOcean

ไม่ต้องเขียนใหม่ storage เป็นไฟล์ล้วน

```bash
rsync -a data/ root@droplet:/data/
# แล้วตั้ง env ชุดเดียวกับใน render.yaml
```
