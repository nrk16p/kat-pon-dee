# comma-separated; "*" is fine for local dev, never in production
CORS_ORIGINS=http://localhost:5180,http://192.168.1.133:5180
# where original captures are kept for model retraining
CAPTURE_DIR=data/captures
# set to 0 to stop retaining captures (you will not get a training set)
RETAIN_CAPTURES=1
